# Python Dependencies - Ye packages install karne honge
# Command: pip install -r requirements.txt

Flask==3.0.0           # Web server framework - API banane ke liye
Flask-CORS==4.0.0      # Cross-Origin requests allow karne ke liye (frontend se connect hone ke liye)
# watchdog==3.0.0      # Removed - Not needed in production (causes issues on some Python versions)
